/*
Why doesn't this code work? Tryo to make it work
and guess what the result will be
(hint: this is a little bit of review from the last lesson).
*/

// const numberVariable = 0;

// numberVariable ++;
// numberVariable ++;
// numberVariable ++;

// console.log(numberVariable);

//  This code does not work because we define a varaible as a const and then try applying the increment operator to it

// This is the adjusted code

let numberVariable = 0;

numberVariable ++;
numberVariable ++;
numberVariable ++;

console.log(numberVariable);

// this will return 3 as the final value because the code increments by 1 three times
