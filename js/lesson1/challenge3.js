/*
    What does 'resultsVariable' equal? What data type is i?

    We haven't covered everything here in detail yet, but 
    research this for a bit and see what you can find
*/

const variable1 = 20;
const variable2 = '40';

const resultVariable = variable1 + variable2;

console.log("output: ", resultVariable);

/* 
The output of this code is 2040.

In JavaScript, the + operator is used for both numeric addition and string concatenation. When 
you "add" a number to a string the interpreter converts your number to a string and concatenates 
both together.
*/
